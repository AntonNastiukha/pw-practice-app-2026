pipeline {
    agent any

    stages {
        stage('Environment') {
            steps {
                echo 'Current workspace information'

                sh 'pwd'
                sh 'ls -la'
            }
        }
    }
}